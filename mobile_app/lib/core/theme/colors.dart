import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const MaterialColor primary = Colors.grey;
  static const MaterialColor primaryVariant = Colors.grey;

  // Secondary Palette

  // Neutrals
  static const MaterialColor invalidFormField = Colors.red;
  static const MaterialColor validFormField = Colors.green;
  static const Color white = Colors.white;
}
